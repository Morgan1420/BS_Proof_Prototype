/**
 * Route/param definitions for the root Stack Navigator. None of the
 * current screens take params, but keeping this centralized makes it easy
 * to add typed params later (e.g. LibraryScreen filtering by category).
 */
export type RootStackParamList = {
  HomeScreen: undefined;
  ScanScreen: undefined;
  LibraryScreen: undefined;
};
